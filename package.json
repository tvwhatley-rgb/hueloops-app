{
  "name": "hueloops-app",
  "version": "1.0.0",
  "description": "HueLoops Premium App",
  "main": "index.js",
  "scripts": {
    "build": "if [ -f index.html ]; then mkdir -p www && cp index.html www/; else echo 'index.html already in www directory, skipping copy'; fi"
  },
  "keywords": [],
  "author": "Hue Assumptions",
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor-firebase/authentication": "^6.0.0"
  }
}